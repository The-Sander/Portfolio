public build is found in the dist folder. This might not run locally, please run following commands:

run: `nmp i` to install all dependencies
run: `npm run dev` to run a local server




`npm run build` to build a dist (this is what's uploaded to netlify)
